# このファイルは "src/web/wifi" を「Pythonのパッケージ」として扱うための
# 目印ファイルです。
#
# Pythonでは、フォルダの中に __init__.py という名前のファイルを置くと、
# そのフォルダを import できる「パッケージ」として認識してもらえます
# （例: from web.wifi.routes import WIFI_BP のように、
#  他のファイルから "web.wifi" という名前で参照できるようになる）。
#
# 中身が空でも構いません。実際のルート定義は routes.py に書きます。
# （src/web/auth フォルダも同じ構成になっているので、それに合わせています）
