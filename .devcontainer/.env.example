# Claude Code(Bedrock 経由)用の秘匿情報テンプレート。
# このファイルを .env にコピーし、配布された Bedrock APIキーを記入してください。
#   cp .devcontainer/.env.example .devcontainer/.env
# .env は git 管理外です。キーは他人に共有しないでください。

# 配布された Bedrock APIキー(メンターから DM で受け取った値)
AWS_BEARER_TOKEN_BEDROCK=
